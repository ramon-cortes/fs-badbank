export default function Balance() {
  //const logStatus = useContext(ValueContext);
  //console.log(logStatus);
  
  return (
    <div>
      Balance page (Deprecated)
      <br />
      Use "All Data" page
    </div>
  );
}